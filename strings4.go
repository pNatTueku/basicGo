// HastPrefix() function: to see if a string starts with a specifc substring

package main

import (
	"strings"
)

func main() {
	strings.HasPrefix("test", "te")
	strings.HasPrefix("east", "se")
	strings.ToLower("Natty")
	strings.ToUpper("natty")
	strings.Count("LP")
}
